apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-rbac-cm
    app.kubernetes.io/part-of: argocd
data:
  policy.csv: |
    p, role:ci-role, applications, get, java-app, allow
    p, role:ci-role, applications, sync, java-app, allow
    g, jenkins, role:ci-role
  policy.default: role:readonly
